@if(User.Identity.IsAuthenticated){
    <span>@User.Identity.Name, Sesión Iniciada <a asp-page="/Seguridad/logout">Cerrar sesión</a></span>
}
else{
    <span>Usuario no autenticado,
    <a asp-page="/Seguridad/login">Iniciar Sesión</a>
    </span>
}